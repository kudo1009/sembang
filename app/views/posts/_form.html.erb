<%= form_for @post, url: {action: 'confirm'} do |f| %>
  
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit '投稿する' %>
  </div>
  
<% end %>