<%= form_with(model: @post, local: true ,url: choose_new_or_edit ) do |form| %>
  
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit '投稿する' %>
  </div>
  
<% end %>